# Production Environment Configuration Template
# Copy this file to .env.production and fill in your values

# ===== SECURITY =====
# Generate a secure SECRET_KEY using: python manage.py shell
# >>> from django.core.management.utils import get_random_secret_key
# >>> print(get_random_secret_key())
ELIBRARY_SECRET_KEY=your-generated-secret-key-here-min-50-chars

# Enable production mode (activates all security settings)
ELIBRARY_PRODUCTION=True

# Disable debug mode in production
ELIBRARY_DEBUG=False

# ===== NETWORK =====
# Comma-separated list of allowed hosts (required in production)
ELIBRARY_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,api.yourdomain.com

# HSTS settings (HTTP Strict Transport Security)
# Recommended: 31536000 (1 year)
ELIBRARY_HSTS_SECONDS=31536000

# Referrer policy for privacy
# Options: no-referrer, no-referrer-when-downgrade, same-origin, 
#          origin, strict-origin, origin-when-cross-origin, 
#          strict-origin-when-cross-origin, unsafe-url
ELIBRARY_REFERRER_POLICY=strict-origin-when-cross-origin

# ===== DATABASE =====
# Format: driver://user:password@host:port/database
# PostgreSQL (recommended):
DATABASE_URL=postgresql://elibrary_user:secure_password@localhost:5432/elibrary_db

# MySQL alternative:
# DATABASE_URL=mysql://elibrary_user:secure_password@localhost:3306/elibrary_db

# ===== CELERY & REDIS =====
# Redis connection for Celery task broker
CELERY_BROKER_URL=redis://:password@localhost:6379/0
CELERY_RESULT_BACKEND=redis://:password@localhost:6379/1

# ===== EMAIL =====
# SMTP configuration for sending emails (password resets, notifications, etc.)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-app-specific-password
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# ===== LOGGING =====
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
DJANGO_LOG_LEVEL=INFO

# ===== OPTIONAL: MONITORING =====
# Sentry error tracking
SENTRY_DSN=https://your-sentry-key@sentry.io/your-project-id

# New Relic application monitoring
NEW_RELIC_CONFIG_FILE=/path/to/newrelic.ini

# ===== OPTIONAL: AUTHENTICATION =====
# For social authentication, API keys, etc.
GOOGLE_OAUTH_CLIENT_ID=your-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret

GITHUB_OAUTH_CLIENT_ID=your-client-id
GITHUB_OAUTH_CLIENT_SECRET=your-client-secret

# ===== OPTIONAL: CDN & STATIC FILES =====
# AWS S3 for static files/media
AWS_ACCESS_KEY_ID=your-aws-key
AWS_SECRET_ACCESS_KEY=your-aws-secret
AWS_STORAGE_BUCKET_NAME=your-bucket-name
AWS_S3_REGION_NAME=us-east-1

# ===== OPTIONAL: BACKUP =====
# Backup service credentials (e.g., AWS S3, Azure Blob, etc.)
BACKUP_S3_BUCKET=your-backup-bucket
BACKUP_RETENTION_DAYS=30
